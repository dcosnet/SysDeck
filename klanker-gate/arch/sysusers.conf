# State + cache home for the gateway user. Deno resolves DENO_DIR through
# $HOME/.cache/deno, so the module cache survives reboots in /var/lib.
u klanker - "Frosty Deno LLM gateway" /var/lib/klanker-gate
